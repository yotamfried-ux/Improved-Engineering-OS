# Capability taxonomy (D28).
#
# GENERATED by tools/capability-seed. Do not edit by hand.
# Regenerate with: pnpm capabilities:emit
#
# Seeded from the legacy Engineering-OS capability registry at the exact revision
# recorded below. Ids are verbatim; enforcement fields are dropped, because the old
# runtime gate is explicitly not inherited (D16, R-04).
#
# Growth rule (D28): capabilities are added only through a promotion PR. Before Stage 10
# that is the C-04 bootstrap path -- an import tool writes a local bundle and the owner
# opens the PR; the tool never pushes.

schema_version: "1"
stability: development
introduced_in: "0.1.0"
deprecated_in: null
replacement: null
migration_path: null

seed:
  status: seeded
  source:
    repository: "yotamfried-ux/Engineering-OS"
    path: "core/capability-registry.yaml"
    revision: "4d517840f18d8c1699a95110e0790d6919450ba4"
    source_digest: "sha256:a693659714c54de27444920af32bd71bd451bbba874c1ab177e20762ff762fc8"
  dropped_fields:
    - "runtime_status"
    - "runtime_enabled"
    - "runtime_scope"
    - "coverage_contract"
    - "selection_rules"
    - "task_classes"
    - "next_pr_contract"
    - "legacy_compatibility"
    - "rejected_for_now"
    - "removed_external_skills"
  note: >-
    Ids are carried over verbatim (D28, Appendix A). Enforcement fields are dropped: the old
    runtime gate is not inherited (D16), and old workflow policy must never become a runtime
    rule. Growth from here is by promotion PR only.

# The kind vocabulary, carried over from the source registry's source_classes.
kinds:
  - id: "connector"
    description: "External service, MCP connector, or source tool."
  - id: "llm_accelerator"
    description: "Supporting model/runtime backend."
  - id: "pattern"
    description: "Reusable implementation pattern under patterns/."
  - id: "skill"
    description: "Claude workflow capability under external-skills/."
  - id: "source_of_truth"
    description: "Repo file, spec, official doc, or project-management record."
  - id: "template"
    description: "Reusable project, hook, command, connector, settings, or workflow template."
  - id: "validator"
    description: "Local or CI check that verifies a required condition."

# 28 capabilities, sorted by id.
capabilities:
  - id: "accelerator.nemotron-optional"
    kind: "llm_accelerator"
  - id: "connector.notion-progress-tracking"
    kind: "connector"
  - id: "pattern.relevant-patterns-checked"
    kind: "pattern"
  - id: "plan.route-plan-before-write"
    kind: "validator"
  - id: "registry.mcp-connector-selected"
    kind: "connector"
  - id: "registry.service-connector-selected"
    kind: "connector"
  - id: "routing.task-router-read"
    kind: "source_of_truth"
  - id: "skill.claude-mem"
    kind: "skill"
  - id: "skill.graphify"
    kind: "skill"
  - id: "skill.rtk"
    kind: "skill"
  - id: "skill.security-review"
    kind: "skill"
  - id: "skill.superpowers"
    kind: "skill"
  - id: "skill.ui-ux-pro-max"
    kind: "skill"
  - id: "source.context7-when-available"
    kind: "connector"
  - id: "source.github-repo-read"
    kind: "connector"
  - id: "source.official-docs-checked"
    kind: "source_of_truth"
  - id: "spec.project-requirements-captured"
    kind: "source_of_truth"
  - id: "template.project-template-checked"
    kind: "template"
  - id: "validation.actions-checked"
    kind: "validator"
  - id: "validation.coderabbit-policy"
    kind: "validator"
  - id: "validation.mcp-opt-in-only"
    kind: "validator"
  - id: "validation.no-broad-mcp-default"
    kind: "validator"
  - id: "validation.policy-change-has-validator"
    kind: "validator"
  - id: "validation.profile-shape-checked"
    kind: "validator"
  - id: "validation.review-checked"
    kind: "validator"
  - id: "validation.security-gate-checked"
    kind: "validator"
  - id: "validation.user-merge-approval"
    kind: "validator"
  - id: "workflow.workflow-read"
    kind: "source_of_truth"
